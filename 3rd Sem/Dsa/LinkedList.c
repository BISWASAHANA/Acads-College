#include <stdio.h>
#include <stdlib.h>

// menu driven program to implement insertion, deletion and display in a linked-list

int main{
    int ll[],int m, int n, int r, int l;

}

void main{
    printf("What do you want to do in the linked-list: \n 1.Insert \n 2.Delete \n 3.Display \n 4.Exit");
    int choice,data,pos;
    scanf("%d",choice);
    switch(choice){
        case 1:
        printf("Enter data to insert: ");
        scanf("%d",data);
        case 2:
        printf("Enter data to delete: ");
        scanf("%d",data);
        case 3:
        printf("The linked list is: ");
        
    }

    
}